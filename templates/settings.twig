{% import "_includes/forms" as forms %}

<h2>Knock, knock ... who's there?</h2>
<p>Share this password with anyone you want to access your website.</p>
{{ forms.textField({
    label: 'Password',
    instructions: 'Minimum 8 characters, don\'t make it easy.',
    id: 'password',
    name: 'password',
    value: settings['password'],
    errors: settings.getErrors('password')})
}}
{{ forms.textField({
    label: 'Whitelisted IP adresses',
    instructions: 'A comma separated list of IP adresses.',
    id: 'whitelist',
    name: 'whitelist',
    value: settings['whitelist'],
    errors: settings.getErrors('whitelist')})
}}
